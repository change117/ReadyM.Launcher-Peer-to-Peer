# ReadyM.Launcher-Peer-to-Peer
Allows you to use the MP launcher, but not connect to their servers. You are connected Peer to Peer
UDP broadcast-based peer discovery (port 25565)
Auto-detection of game executables across platforms
JSON configuration with CLI and interactive modes
Zero external dependencies (stdlib only)
